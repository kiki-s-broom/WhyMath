# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: HARN-170
title: "tests/harness가 실제 이벤트 대장에 policy_warn을 쓴다 — claim 걸린 세션에서 check-edit 훅 테스트가 가짜 경고 7줄을 남김"
track: infra-debt
stage: S3
subject: cross
layer: infra
status: todo
priority: 3
eos_priority: P2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 격리 — tests/harness/test_jit_rules.py::TestCheckEditHook(및 같은 형태로 실제 REPO_ROOT에서 check-edit를 호출하는 테스트 전부 — test_cli.py·test_amend_cli.py·test_incidents.py를 전수 확인)가 실제 backlog/events/<세션>.ndjson에 policy_warn 이벤트를 쓰지 않게 한다(이벤트 경로를 tmp로 돌리거나 조율 정책 검사를 테스트에서 격리). ② 회귀 동결 — claim이 걸린 브랜치에서 tests/harness 전체를 돌린 전후로 backlog/events/ 전 파일의 바이트가 같음을 단언하는 테스트(주입 확인: 격리를 제거하면 RED). ③ test_broken_index_never_blocks_the_edit가 실제 jit 인덱스 파일을 덮어썼다 복원하는 방식도 tmp 사본으로 바꾼다(중단 시 실파일 손상 위험)."
  - "④ [2026-09-25 실측 · 판정 기준 main 3a7a7315] 발생 조건이 ① 문면보다 넓다 — claim이 없는 세션에서도 난다. 원격 claim 캐시에 scripts/harness/backlog.py를 paths로 가진 타 세션 태스크(이번엔 HARN-121)가 있으면 path_overlap이 발화해, 핫 경로 3테스트가 가짜 경고 3줄을 실제 세션 샤드에 쓴다(claim이 있으면 scope_drift가 더해져 7줄). 격리 대상은 claim 유무가 아니라 check-edit의 모든 위반 경로다."
  - "⑤ 기커밋 오염 처리 — main 대장에 같은 서명의 묶음이 이미 74건 들어 있다(12초 안에 backlog.py 경고가 규칙별 3건씩 + 선택적 README.md scope_drift 1건 · 이벤트 350건 · 샤드 31개 · 2026-09-07~09-25). 전체 policy_warn 27,088건의 1.3%라 통계 왜곡은 작지만 거짓 기록이다. 대장은 append 전용이므로 삭제하지 말고 policy report 집계에서 식별·제외하는 방법을 정한다(서명 판별 규칙을 코드로 두고, 판별기가 0건을 내면 실패로 친다). 같은 날 발견한 전파 경로: 세션 종료 훅(~/.claude/stop-hook-git-check.sh)이 '미커밋 변경을 커밋·푸시하라'고 요구해, 테스트가 남긴 가짜 줄이 커밋 대상으로 밀려 올라간다."
artifacts: []
paths:
  - "tests/harness/**"
session: null
notes: "출처: 2026-09-24 MP-02 재회차 세션(claude/wizardly-allen-bqgujn)에서 CI 재현으로 tests/harness를 두 번 돌렸는데 두 번 다 세션 이벤트 대장에 가짜 policy_warn 7줄이 생겼다(scope_drift 4 · path_overlap 3 — 파일 scripts/harness/backlog.py·README.md). 줄 수가 TestCheckEditHook의 호출 구성과 정확히 일치한다: backlog.py 경로 3회 × (scope_drift+path_overlap) + README.md 1회 × scope_drift. 원인: 이 테스트가 monkeypatch.chdir(REPO_ROOT) 후 실제 check-edit를 호출하므로, 실행 세션에 claim이 있으면 조율 정책 검사가 실제로 발화해 실제 대장에 쓴다. 피해: 커밋하면 대장에 거짓 경고가 남는다 — 이번에는 두 번 다 커밋 전에 발견해 되돌렸다(백업 보존). claim 없는 CI 러너에서는 증상이 없어 CI가 구조적으로 못 잡는다.\n\n[정정 2026-09-25] 2026-09-25 재발(이 세션 · claude/adoring-bohr-54tyhd) — ci_mirror로 harness-integrity를 재현하자 세션 샤드에 가짜 path_overlap 3줄(23:10:51·53·57, file=scripts/harness/backlog.py)이 생겼고, 세션 종료 훅이 그것을 커밋하라고 요구했다. 커밋본과 바이트 대조로 3줄만 제거했다. claim 없는 세션이었으므로 발생 조건을 넓히고, 이미 커밋된 오염 규모를 실측해 acceptance ④⑤로 덧붙인다. 이 shallow 클론의 git log -S는 TestCheckEditHook 도입을 2026-09-22로 보이지만 서명 묶음은 09-07부터 있다 — 같은 형태의 다른 테스트가 있거나 이력이 잘렸다(미판정 · ① 전수 확인 범위)."
updated: 2026-09-25
