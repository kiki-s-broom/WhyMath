# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: EOS-20-residual-leak-guard-message-order
title: "잔여 누수 가드가 1홉 경로에서 설계된 메시지 대신 IndexError로 죽는다 — 단언 순서 결함(진단성)"
track: infra-debt
stage: S3
subject: cross
layer: infra
status: todo
priority: 3
eos_priority: P2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 순서 결함 실측·수정 — test_residual_transitive_leaks_are_frozen_and_only_shrink 가 관측 집합을 만들 때 path[-2] 를 먼저 읽고, 그 다음에야 len(path) >= 2 를 단언한다. 1홉 잔여 경로가 실제로 나오면 설계된 메시지(ADAPTER 직접 import는 EOS-67이 막는다) 대신 IndexError 로 죽는다. 단언을 집합 구성 앞으로 옮기고 그 순서를 회귀 테스트로 동결한다.\n② 실패 주입으로 확인 — 1홉 잔여 경로를 담은 합성 result 픽스처를 주입해 ⓐ 수정 전에는 IndexError ⓑ 수정 후에는 설계된 AssertionError 메시지가 나오는지 양방향 대조한다. 메시지 문자열이 실제로 출력에 실리는지까지 본다.\n③ 범위 한정 — 이 태스크는 진단성(왜 RED인지 읽히는가)만 다룬다. 가드의 판정 자체는 양쪽 다 RED이므로 보호 강도는 바뀌지 않는다. baseline 값·프로브 알고리즘은 건드리지 않는다."
artifacts: []
paths:
  - tests/infra/test_eos_core_boundary_probe.py
session: null
notes: "출처: EOS-117(P-12) 위반 주입 실측 중 발견(2026-09-19). CORE(lang)->ADAPTER 직접 import 를 주입하자 프로브 테스트 3건이 RED였는데, 그중 1건이 AssertionError 가 아니라 IndexError: list index out of range (tests/infra/test_eos_core_boundary_probe.py:153) 로 죽었다. 침묵 실패는 아니므로(RED는 RED다) 보호 자체는 성립하지만, 그 가드가 사람에게 무엇을 고치라고 말하려던 문장이 출력되지 않는다. EOS-117 범위 밖이라 분리 등재한다(범위 규율 — 김에 같이 고치지 않는다)."
updated: 2026-09-19
