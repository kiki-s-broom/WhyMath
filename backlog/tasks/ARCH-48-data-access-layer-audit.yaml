# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ARCH-48-data-access-layer-audit
title: "DB 직접 접근 감사 축 부재 — Repository 계층 없이 api·l1~l4가 ORM 세션을 직접 잡는데 그것을 재는 계약·감사가 0건"
track: infra-debt
stage: S3
subject: cross
layer: backend
status: done
priority: 2
eos_priority: P2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 처분 판정 먼저 (배선이 아니라 판정이 결론): 데이터 접근 계층을 (a) Repository 계층 신설로 수렴 (b) 현행 세션 직접 접근을 관례로 인정하고 baseline 동결 + 신규 증가만 차단 (c) 계층별 차등(api는 금지·l1~l4는 허용) — 셋 중 하나를 실측 규모로 고른다. 위반이 많으면 (a)는 만료 없는 유예를 부르므로 (b)/(c)의 baseline 계약이 필요할 수 있다"
  - "② 실측 전제 (2026-09-15 · main 4b33d243): Repository 계층 모듈 0건(`find -name \"*repositor*\"` → 0). ORM 세션 직접 조작(`session.(query|execute|scalars|add|commit)`) 파일 수 = api 12 · l1 6 · l2 15 · l3 2 · l4 4 · db 1 · harness 11 · ops 8. api는 42파일 중 15파일이 해당(users·gating·curricula·auth·coach·study·problems·me·reports·interactions·concepts 등). 원시 SQL `text(\"…\")` 150건(마이그레이션·테스트 제외 필터 적용). 이 수치는 판정의 출발점이지 결론이 아니다 — 첫 단계에서 AST 기반으로 재측정한다(정규식은 문자열·주석을 센다)"
  - "③ 집행 지점을 정본화와 별항으로 적는다: import-linter 계약 2축(7계층 단방향 · EOS Core→Adapter 금지)은 **데이터 접근 축을 보지 않는다**(pyproject.toml `[tool.importlinter]` 전수 확인). 판정 결과를 무엇이 기계로 막는지 명시한다 — forbidden 계약 추가인지, `tests/infra/` 전수 거버넌스 테스트인지, 둘 다인지"
  - "④ 보호 장치는 실패 주입으로 검증한다: 새 가드를 넣으면 금지 상태(예: api 신규 파일에서 session.execute 호출)를 실제로 주입해 RED를 확인한 뒤에만 보호로 친다. 정상 입력에서 초록인 것은 증거가 아니다. baseline 동결을 고르면 만료·재확인 지점을 반드시 동반한다"
  - "⑤ 출처와 소유 공백: 이 갭은 `docs/reviews/eos_source_docs_gap_review_2026-08-31.md:154` 36번 행(\"Repository 계층 — ORM 직접 접근이 서비스 계층에 남아 있음(부분)\")으로 이미 관측됐으나 B급 미등재 후보로 남아 2주간 소유자가 없었다. 이 태스크가 그 소유자다"
artifacts:
  - "PR #1188 https://github.com/kiki-s-broom/WhyMath/pull/1188 · 커밋 807a60da(감사·처분) + 024f29f6(E501 정정) + 3d6deb9b(main 병합) · 판정 (b) baseline 동결 + 신규 증가만 차단 — (a)는 118건 즉시 위반으로 만료 없는 유예를 부르고 (c)는 api 14파일 때문에 실측상 (b)로 퇴화 · 재측정이 기존 실측을 뒤집었다(정규식 약 40파일 → AST 118파일 507호출, 엔진 직접 연결 conn 축 l1 28파일 누락 발견) · 정본 docs/architecture/data_access_layer_disposition.md(판정 기준 해시 명기) + 계측기 scripts/analysis/data_access_layer_audit.py(baseline 118파일 계층별 집합·스캔 0건이면 exit 1) + 집행 2축(import 축 = pyproject forbidden 계약 신설 4 kept 0 broken · 호출 축 = tests/infra 가드 13건) + 만료 2겹(기계 ratchet · BASELINE_REVIEW_BY 2027-01-31) · 뮤테이션 19회차(주입 15 + 대조군 4) 생존 0건 세션 직접 재현 · whymath_backend 소스 0줄 변경(행동 변경 0) · 전체 백엔드 12787 passed EXIT=0 커버리지 90.46% · 계층 커버리지 PASS · ruff/black 3범위 전건 + mypy --strict(669파일) + lint-imports EXIT=0 · tests/infra+harness 2255 passed · validate/audit-deps green · 후속 권고 3건 보고(api 14파일 제거·Repository 재판정·동적 접근 축 보강)"
paths:
  - scripts/analysis/data_access_layer_audit.py
  - src/backend/pyproject.toml
  - tests/infra/test_data_access_layer_contract.py
session: null
notes: "출처: Kiki 지시(2026-09-15) \"소유자 없는 갭 등재\" 8건 중 'DB 직접 접근 감사' 축. 원 관측은 eos_source_docs_gap_review_2026-08-31.md:154(36번 행·B급 미등재 후보)이며, 그 문서 §5가 B급 8건을 \"Kiki 판정 대기\"로 남긴 뒤 소유자 없이 떠 있었다 — CLAUDE.md \"소유자 없는 알려진 결함을 만들지 않는다(Gate D)\"의 미이행 구간.\n\n부재 판정 범위(명시): 소유자 0건은 역할 기반 검색 결과다 — backlog/tasks 전수에 대해 repository / Repository 계층 / ORM 직접 / 직접 접근 / db.session / 세션 주입으로 검색했고, 걸린 파일들은 전부 git 저장소(repository) 문맥이었다. \"내가 찾은 방법으로는 0건\"이라는 뜻이다.\n\n축 구분: 7계층 단방향 계약(import-linter)·EOS Core↔Adapter 경계 계약과는 **다른 축**이다. 그 둘은 모듈 간 import 방향을 보고, 이 축은 데이터 접근 수단(세션·원시 SQL)이 어느 계층에 노출되는지를 본다. 중복 등재가 아니다.\n\n[정정 2026-09-15] paths ['src/backend/pyproject.toml', 'src/backend/whymath_backend/api/*.py', 'tests/infra/*.py'] → ['scripts/analysis/data_access_layer_audit.py', 'src/backend/pyproject.toml', 'tests/infra/test_data_access_layer_contract.py']: 등재 시 선언한 api/*.py·tests/infra/*.py가 겹침 경보 45건을 내 상시 오탐이 됐다(상시 오탐은 병렬 세션이 경보를 무시하게 만든다) — 1차 산출(감사 스크립트·import-linter 계약·전용 거버넌스 테스트) 범위로 좁힌다. 서빙 코드 수정은 ①의 처분 판정 이후 단계이며 그때 paths를 다시 넓힌다\npaths 제거 2건: ['src/backend/whymath_backend/api/*.py', 'tests/infra/*.py']\n\n[개명 2026-09-16] ARCH-46-data-access-layer-audit → ARCH-48-data-access-layer-audit: 번호 충돌 해소 — main에 ARCH-46-router-only-contract-enforcement가 먼저 착지했다(PR #1166 계열). 등재 시점(2026-09-15 08:33)에는 로컬·원격 claim 어디에도 없어 CLI가 ARCH-46을 배정했고, 그 add가 출력한 가시성 고지(\"이 번호는 push 전까지 다른 세션에 보이지 않는다\")가 그대로 실현됐다. 머지 큐 빌드 run 35043102734의 harness-integrity 잡 step 5(backlog 무결성 검증)가 exit 1로 이 충돌을 잡아 PR이 큐에서 이탈했다 — 검증기가 지시한 대로 미머지인 이쪽을 개명한다(HARN-100). 내용·acceptance·paths는 불변이며 식별자만 바뀐다."
updated: 2026-09-17
