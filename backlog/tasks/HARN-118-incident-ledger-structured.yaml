# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: HARN-118-incident-ledger-structured
title: "사고 대장 구조화 — 사고 676건·규칙 52건의 회차 계수를 단일 대장으로, 2회차부터 코드 착지 강제"
track: infra-debt
stage: S4
subject: cross
layer: infra
status: review
priority: 2
eos_priority: P2
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - "① 구조화 대장 신설 — `backlog/incidents.yaml`(또는 ndjson) + `backlog.py incident add|report|series` 서브커맨드. 필드 = date·cat(A~K, 정의는 `docs/data/recurring_failure_ledger_2026-09-20/schema.md`)·series_id·nth(자동 계산)·damage_class·fix_form·fix_ref·who_caught·quote. 손편집 금지(validate가 스키마 검사)."
  - "② 2회차 코드 착지 강제 — 같은 series_id의 2회차 이상을 `incident add`로 등재할 때 fix_form이 rule(산문)뿐이면 exit 1로 거부하고, code 또는 task 참조(테스트 파일 경로·HARN/OPS 태스크 ID)를 요구한다. 뮤테이션: 2회차 rule-only 등재 주입 → RED, code 참조 부착 → GREEN. 거부 사유가 stderr에 series·nth와 함께 남는다."
  - "③ 시드·리포트 — `docs/data/recurring_failure_ledger_2026-09-20/incidents.jsonl` 676건을 시드로 적재(사람 검수 열 `reviewed: false` 기본). `incident report`가 대분류·월·피해·대책 형태·계열 최대 회차 표를 낸다(보고서 §2 표와 같은 형식·재계산 일치 검증). `weekly_metrics_report`에 사고 건수·계열 최대 회차·산문만 대책 비율 3종 append 배선(정본화≠집행 분리 — 집행 지점 = weekly-metrics 워크플로 스텝에서 실제 호출됨을 `tests/infra`가 동결)."
artifacts: []
paths:
  - "backlog/incidents.*"
  - scripts/harness/incidents.py
  - scripts/harness/backlog.py
  - "tests/harness/test_incidents*.py"
  - "docs/data/recurring_failure_ledger_2026-09-20/**"
  - src/backend/whymath_backend/ops/weekly_metrics_report.py
  - tests/backend/ops/test_weekly_metrics_harness_block.py
  - tests/infra/test_incident_metrics_wiring.py
  - ".github/workflows/weekly-metrics.yml"
session: claude/peaceful-franklin-fo4p3q
notes: "근거 = docs/reviews/recurring_failure_taxonomy_2026-09-20.md §5 P1·§4 M1/M5. 사고 676건·규칙 52건 중 산문만 56%·계열 최대 11회차인데 회차 계수가 문서마다 다르다. 이 태스크가 착지하면 CLAUDE.md 새 산문 규칙 등재 동결(§5.3)이 성립한다. HARN-121(헌법 다이어트)의 사고 경위 이동 대상이 이 대장이다.\n\n[정정 2026-09-21] paths ['backlog/incidents.*', 'scripts/harness/incidents.py', 'scripts/harness/backlog.py', 'tests/harness/test_incidents*.py', 'docs/data/recurring_failure_ledger_2026-09-20/**'] → ['backlog/incidents.*', 'scripts/harness/incidents.py', 'scripts/harness/backlog.py', 'tests/harness/test_incidents*.py', 'docs/data/recurring_failure_ledger_2026-09-20/**', 'src/backend/whymath_backend/ops/weekly_metrics_report.py', 'tests/backend/ops/test_weekly_metrics_harness_block.py', 'tests/infra/test_incident_metrics_wiring.py', '.github/workflows/weekly-metrics.yml']: acceptance 3의 weekly_metrics_report append 배선·집행 지점(워크플로 스텝)·그 배선을 동결하는 tests/infra가 선언 범위 밖이었다 — 범위 확장"
updated: 2026-09-21
