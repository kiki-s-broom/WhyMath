# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ASM-06-distractor-misconception-reverse-link
title: "오답 선택지→오개념 역방향 배선 — 학생이 실제로 고른 보기를 오개념 ID로 (미병합 브랜치 jkwdzn 재작성)"
track: math-completion
stage: S4
subject: math
layer: backend
status: blocked
priority: 4
eos_priority: P1
owner: claude
depends_on:
  - HARN-57-done-artifact-correction-path
requires_gates: []
acceptance:
  - "선행 확인: NLP-02 서버 채점 shadow 경로가 학생이 고른 MC 인덱스를 이미 포착하는지 실측 확인 — 포착 안 하면 이 태스크는 그 확장부터 선행해야 하므로 스코프를 그에 맞게 재정의하고 착수"
  - "포착된 선택 인덱스를 오개념 ID로 매핑 — 오개념을 초기 context에 preload하지 않고 reactive retrieval만 사용(CLAUDE.md 원칙)"
  - "[고립 참조 2026-09-07 stray-code 감사] origin/claude/subject-problems-theory-check-7n9n72(621b11f9 · 커밋 087859bd)에 이 태스크의 done 구현이 보존 — selected_choice_index 슬롯 + distractor_map 대조 + alembic 20260810_1200_0afd40ce1867_attempt_selected_choice_index.py + l4/misconception/distractor_link.py + tests/backend/l4/misconception/test_distractor_link.py(main 부재 실측·심볼 grep 0). 위 [차단 2026-08-11]의 '재정의 방향'(요청 슬롯 selected_choice_index 신설+적재+마이그레이션)과 동형이며 main harness/distractor_signal_dormancy_report.py의 정본명과 일치한다. unblock 후 착수 시 재구현 말고 이 구현을 파일 단위 대조 후보로 우선 검토(브랜치가 228커밋 낡아 통째 머지 금지). 회수 판정 전 원 브랜치 삭제 금지"
  - "[고립 참조 보완 2026-09-07 stray-code 8회차 · 판정 기준 main d50781b7] 위 참조의 3파일만으로는 회수가 성립하지 않는다. **동반 변경 전건(main 부재 실측)**: schema/activity.py selected_choice_index 필드(int|None·ge=0) · db/models/activity.py ProblemAttempt 컬럼 · api/me.py AttemptSubmitRequest 슬롯 신설(extra=forbid라 클라가 임의 필드로 못 보냄)+적재+응답 matched_misconception_id · db/schema_version.py 리비전 등재 · l4/misconception/__init__.py resolve_misconception_from_choice export + 테스트 3파일(tests/backend/api/test_me.py 6건·tests/backend/db/test_activity_orm.py·tests/backend/schema/test_activity.py). 실측: main src에서 resolve_misconception_from_choice·matched_misconception_id 0파일, ORM·alembic의 selected_choice_index 0(유일 hit는 harness/distractor_signal_dormancy_report.py의 예약 슬롯명). **alembic 재채번 필수** — 브랜치 리비전 0afd40ce1867의 down_revision은 7ef2b5a8e69e(S3-32 구설계)인데 main은 #738에서 review_turns_remaining+attempt_id 존재 판정으로 대체하며 그 리비전을 건너뛰었다(폐기). 그대로 포트하면 multiple heads — main 단일 head c1a5e07b4d38(93 리비전·2026-09-07 실측) 위로 재채번한다. **paths 유의**: 현 paths(l4/misconception/**·schema/problem.py)가 위 파일들을 덮지 않아 착수 시 scope-drift 경고가 나면 오탐이 아니라 이 항이 명시 승인한 범위다(paths 정정 CLI는 HARN-57 소관·미착지)"
  - "[Codex P1 수용 2026-09-07 · 실측] 위 보완이 열거한 api/me.py(AttemptSubmitRequest) 경로만 회수하면 **실제 학생 흐름에서 슬롯이 영원히 NULL**이라 역방향 링크가 휴면으로 남는다. 실측: 모바일은 POST /v1/me/attempts를 **부르지 않는다**(completion_signal.dart:9,11·coach_models.dart:402가 'api/coach.py 계약 명문·중복 적재 금지'로 자인) — 선지 탭은 chat_screen.dart::_onChoiceSelected가 student_input에 **값만** 실어 coach로 보내고, ProblemAttempt는 api/coach.py::_complete_problem(ProblemAttemptORM 생성부 :1040)이 만든다. 따라서 회수 범위에 **coach 제출 경로를 포함**한다 — ⓐcoach 요청 스키마에 선택 인덱스 슬롯 신설 + _onChoiceSelected가 인덱스 동봉, 또는 ⓑ서버가 student_input 값과 선지 목록을 대조해 인덱스 파생(택1을 착수 시 판정하고 근거를 notes에 기록). **E2E 테스트로 '선지 탭 → attempt.selected_choice_index 적재 → 오개념 링크 산출'을 관통 증명**한다. me.py 경로는 API 소비자(웹·외부)용으로 함께 유지. **paths 집행 정정**: 앞 항의 'scope-drift 경고는 승인된 범위' 문면은 *다른 세션에게* 충돌을 알리지 못한다(start·overlap은 paths만 읽는다) — 그래서 이 태스크를 HARN-57(amend --path 신설)에 depends_on으로 걸었다. 착수 전에 paths를 api/**·db/models/**·schema/**·alembic/**로 실제 확장한다"
artifacts: []
paths:
  - "src/backend/whymath_backend/l4/misconception/**"
  - src/backend/whymath_backend/schema/problem.py
session: null
notes: "브랜치 jkwdzn의 원 ASM-04/D5 재작성. REC-02(오개념 표적 프로브 공급)가 정방향(가설→프로브 선택)만 다룸 — 이건 역방향(학생이 고른 실제 오답 인덱스→오개념 ID). 선행조건: 현재 student_answer가 자유텍스트라 '어느 보기를 골랐는지' 자체가 채점 레이어에 없음 — NLP-02 shadow 채점이 선택 인덱스를 이미 포착하는지 먼저 확인 후 스코프 확정.\n\n[차단 2026-08-11] 선행 조건 불충족 확정 + 스코프 재정의 필요 (2026-08-11 r2 §3 D4 실측). acceptance ①은 'NLP-02 shadow가 학생이 고른 MC 인덱스를 이미 포착하는지 실측 확인 — 포착 안 하면 스코프를 그에 맞게 재정의하고 착수'를 선행으로 걸었다. 실측 답 = **포착하지 않는다**. ⑴shadow(harness/attempt_grading_shadow_report.py)는 student_answer 자유텍스트를 verify_answer에 넘길 뿐 선지 개념 자체가 없다 ⑵애초에 그 shadow의 코퍼스 파생 가능 비율이 0.00%다(ASM-08). 진짜 차단 요인은 채점 권위가 아니라 AttemptSubmitRequest(api/me.py:641-657)에 선택 인덱스 슬롯이 없다는 것 — model_config가 extra='forbid'라 클라가 임의 필드를 실어 보낼 수도 없다. 재정의 방향: 요청 슬롯 selected_choice_index 신설 + 적재 + 클라 변경 + 마이그레이션으로 스코프를 다시 잡고, 사장 규모 관측(ASM-09) 착지 후 해제한다. 승계할 제약 = 오개념 reactive retrieval만(preload 금지)·오답을 오개념으로 단정 금지(distractor_map은 가설 근거)·기존 misconception_hypothesis 좌석으로 흘리고 별도 판정 경로 신설 금지(이중 진실원천)·점수/오답률 노출 금지.\n\n[정정 2026-09-01] eos_priority null → P1: 판정표 R/내용KPI — 12월 검증 관여(KPI·계약·앵커 자산). EOS-80 일괄 백필\n\n[정정 2026-09-07] 7n9n72 브랜치 단위 소유자였던 HARN-37이 done(#962)이 되어 잔여 좌석의 명시 참조가 끊김 — 2026-09-07 미머지 브랜치 감사(docs/reviews/unmerged_branch_audit_2026-09-07.md)가 좌석 태스크 본문에 고립 참조를 부착\n\n[정정 2026-09-07] stray-code 8회차 후속(#1027 §4.2 표 · #1020 머지 후 보완): 7회차가 부착한 고립 참조가 부재 파일 3종(distractor_link·테스트·alembic)만 열거해 동반 변경이 빠졌다 — 그대로 회수하면 슬롯이 없어 RED이고, alembic은 폐기 리비전에 체인돼 multiple heads가 난다. 판정 기준 main d50781b7\n\n[정정 2026-09-07] Codex P1+P2(PR #1033) 수용: ①me.py 경로만 회수하면 실제 학생 흐름(coach)에서 슬롯이 영원히 NULL이라 기능이 휴면 — 모바일이 POST /v1/me/attempts를 부르지 않음을 실측 ②acceptance 산문은 claim 시점 충돌 검출에 무력(start·overlap은 paths만 읽음)이라 paths 정정 CLI(HARN-57)를 depends_on으로 집행"
updated: 2026-09-07
