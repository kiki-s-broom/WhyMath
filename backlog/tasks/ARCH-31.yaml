# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ARCH-31
title: "Content Version 분리: Problem.content_version_id nullable 추가 및 ContentVersion 모델 설계"
track: math-completion
stage: S4
subject: math
layer: backend
status: todo
priority: 3
eos_priority: P1
owner: claude
depends_on: []
requires_gates: []
acceptance:
  - Problem schema/ORM에 content_version_id nullable FK 추가
  - ContentVersion Pydantic 모델 및 ORM 설계
  - Alembic 마이그레이션 추가
  - 기존 identity_id와의 관계 문서화
  - "[게이트 G-eos49-content-version-seat 판정 2026-09-06·D안 파급] problem_version 테이블은 **Problem 좌석**에 편입한다(엔티티 19종 불변·ContentVersion 좌석은 비운 채 유지). acceptance 2항 'ContentVersion Pydantic 모델 및 ORM 설계'의 **ORM 축은 범위 밖**으로 축소 — 범용 ContentVersion 테이블은 canonical_entity_model_v1.md §3-D 부재 동결 + 44 §14-8(Unified Entity 슈퍼테이블 금기) 대상이다. 공통 축은 44 §6.2 VersionHeader **Pydantic 계약**으로만 공유한다(§6.1 Hybrid). §2-A 표·좌석 상수·ORM/alembic은 같은 PR에 원자적으로(§5-1)"
  - "[승계 대조표 2026-09-06 — 5항의 파급을 항별로 확정한다. 아래가 1~2항보다 **우선**하며, 1~2항은 이 표에 적힌 형태로만 읽는다(하네스 acceptance는 append 전용이라 원항 삭제 경로가 없다 — 원항 보존은 HARN-20 규약)] ①항 'content_version_id nullable FK' → **problem_version_id nullable FK**로 읽는다. 대상은 problem_version.version_id이며, 범용 content_version 테이블이 **아니다**(그 이름은 test_canonical_entity_model_freeze.py RESERVED_ABSENT_TABLE_NAMES에 예약돼 생성 시 RED). ②항 'ContentVersion Pydantic 모델 및 ORM 설계' → **ORM 축은 ProblemVersion 하나로 한정**(Problem 좌석 편입), **Pydantic 축은 44 §6.2 VersionHeader 공통 계약**으로 읽는다. 범용 ContentVersion은 Pydantic·ORM 양쪽 모두 범위 밖이다. ③④항은 원문대로 유효(alembic 리비전·identity_id 관계 문서화). **title은 원문이 'ContentVersion 모델 설계'로 남아 있으나 CLI에 title 정정 경로가 없다 — HARN-74로 등재**(대장 손편집 금지·설계 공백은 태스크로)"
  - "[참조 정정 2026-09-06 · 직전 승계 대조표의 오기] 대조표 말미의 'HARN-74로 등재'는 **오기**다. ⓐ HARN-74는 병렬 세션이 선점 중이라 CLI가 거부했다(HARN-10 번호 충돌 검사 실동작) ⓑ title 정정 축의 실제 소유자는 신규 태스크가 아니라 **HARN-57 ⑤**(2026-09-03 기등재)다. 확정 소유: title 정정 경로 = **HARN-57-done-artifact-correction-path ⑤** · acceptance 항 폐기 표시 경로 = **HARN-76-acceptance-supersede-and-title-amend ①**. 이 정정은 ARCH-31의 구현 범위를 바꾸지 않는다(대조표 ①~④항 그대로 유효)"
artifacts: []
paths:
  - src/backend/whymath_backend/schema/problem.py
  - src/backend/whymath_backend/db/models/problem.py
  - "src/backend/whymath_backend/alembic/versions/*"
session: null
notes: "011_1 EOS Identity 수용 결정 후속. 첨부 설계안의 Content ID ≠ Content Version ID 분리를 MVP 범위 내에서 구현.\n\n[정정 2026-09-01] eos_priority null → P1: 판정표 R/F5 — 12월 검증 관여(KPI·계약·앵커 자산). EOS-80 일괄 백필\n\n[정정 2026-09-06] G-eos49 판정의 ARCH-31 파급 — 같은 벽(검사 ①)에 부딪히는 형제 축이라 재판정 방지\n\n[정정 2026-09-06] Codex P2 #1012 수용 — 5항이 1·2항과 배타적이라 구현자가 FK 대상을 결정할 수 없던 모호성 해소. 항별 승계 대조표로 확정\n\n[정정 2026-09-06] 직전 amend의 태스크 ID 오기 정정 — HARN-74 선점·title 축 실소유자는 HARN-57 ⑤"
updated: 2026-09-06
