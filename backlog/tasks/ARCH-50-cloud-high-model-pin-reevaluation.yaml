# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ARCH-50-cloud-high-model-pin-reevaluation
title: "CLOUD_HIGH 모델 핀 재평가 — 목표 Opus 5(2026-09-21 Kiki 지시·Fable 5.1 대체) + 구독 자격증명 사슬 복원"
track: infra-debt
stage: S3
subject: math
layer: backend
status: todo
priority: 3
eos_priority: P2
owner: claude
depends_on:
  - ARCH-62-cloud-cost-table-seat-axis
requires_gates:
  - G-opus5-billing-path-prod-recheck
acceptance:
  - "①단가표·한도표 동시 갱신 — `l3/router.py`의 `CLOUD_TOKEN_PRICE_USD_PER_1M`에 새 핀의 실단가를 넣고, `DAILY_LIMIT_KRW`(free 100·basic 500·premium 2000·gifted 5000)가 여전히 의도한 콜 수를 허용하는지 재계산한다. Fable 5.1은 10/50 USD per 1M로 현행 Opus 4.7(5/25)의 정확히 2배이므로, 실측 p50 토큰(입력 74·출력 358)으로 콜당 14.4원에서 28.7원이 되고 premium 한도가 139콜에서 69콜로 반감한다. 단가만 바꾸고 한도표를 두면 상위 티어가 조용히 절반이 된다.\n②API 표면 차이의 실측 대조 — Fable 5.1은 Opus 계열과 요청 표면이 다르다(사고는 항상 켜짐이라 `thinking` 생략 또는 adaptive만 허용·`budget_tokens`는 400·강제 `tool_choice` any/tool은 400·assistant prefill 400·`output_config.effort`로 깊이 제어·거부 stop_reason 처리와 server-side fallback 권장). `l3/providers/anthropic.py`가 현재 보내는 인자 집합이 새 핀에서 유효한지 실물 SDK 표면으로 확인한다 — 시임 테스트만으로 정합 선언 금지(CLAUDE.md).\n③검증 계약 불변 확인 — 모델이 바뀌어도 SymPy 단일 권위·학생 제공 전 검증·Langfuse 추적·미성년자 데이터 정책·저작권 레일은 유지된다. 교체 후 해당 게이트들이 여전히 green인지 확인한다.\n④MEMORY 결정 로그 기재 — 교체 근거·비용 영향·한도표 재계산 결과."
  - "[목표 핀 변경 2026-09-21] Kiki 지시로 CLOUD_HIGH 목표가 Fable 5.1 → **Opus 5**로 바뀐다. 위 ①~④는 Fable 5.1 전제로 쓰였으므로 아래가 우선한다. ⑤ 정확한 모델 id는 `claude-opus-5`이며 **날짜 접미사를 붙이지 않는다**(컨텍스트 1M). ⑥ 단가는 $5/$25로 현행 핀 `claude-opus-4-7`과 **동일**하다 — 따라서 ①의 단가표 값 변경과 `DAILY_LIMIT_KRW` 재계산은 **불요**다(Fable 5.1의 2배 단가를 전제한 ① 본문은 이 핀에 적용되지 않는다). ⑦ 단, 동작이 다르다 — Opus 5는 thinking이 **기본 ON(adaptive)**이라 `thinking` 생략 시에도 사고가 돌고, 4.7(생략 시 사고 없음) 대비 출력 토큰이 늘 수 있다. 그러므로 `_EST_ASSUMED_OUTPUT_TOKENS=358`(4.7 기준 2026-07-14 실측 p50·n=32)은 교체 후 `ops.cost_report`의 suggested_est_*로 재대입한다. ⑧ `budget_tokens`는 Opus 5에서 제거돼 400을 내므로 현행 프로바이더가 그것을 보내지 않는지 실물 SDK 표면으로 확인한다(②의 실측 대조 요구를 Opus 5 축으로 승계). ⑨ **자격증명 사슬 복원** — `l3/providers/anthropic.py`가 `AsyncAnthropic(api_key=...)`로 키를 명시 주입해 SDK의 해소 사슬(ANTHROPIC_API_KEY → ANTHROPIC_AUTH_TOKEN → OAuth 프로파일 → WIF)을 통째로 가린다. 개발 단계에서 Claude Max 구독 경유로 Opus 5를 쓰려면 키가 비었을 때 인자를 **생략**해 SDK가 사슬을 타게 해야 한다. 조용한 폴백 금지 — 어느 경로로 인증됐는지 /status와 회차 리포트가 auth_source(api_key|auth_token|oauth_profile|none)로 말한다. 변별력: 키만 있는 환경·토큰만 있는 환경·둘 다 없는 환경 세 가지가 서로 다른 값을 내야 하며 셋 전부에서 테스트한다. ⑩ 단가표 구조 변경은 이 태스크가 아니라 ARCH-62(좌석 축 분리)가 소유한다 — 중복 구현 금지."
artifacts: []
paths:
  - src/backend/whymath_backend/l3/router.py
  - src/backend/whymath_backend/l3/providers/anthropic.py
session: null
notes: "착수 배경: 2026-09-16 Kiki 요청(\"high = Fable 5.1\"). 현행 CLOUD_HIGH 핀은 claude-opus-4-7이다(config.py anthropic_model_high). 교체 자체는 env 오버라이드로도 가능하나 단가표·한도표가 코드 상수라 함께 움직여야 하고, Fable 계열은 요청 표면이 Opus와 달라 프로바이더 인자 집합 재확인이 필요하다. 형제 태스크 ARCH-49(DeepSeek)와 달리 이쪽은 같은 Anthropic 관할이라 국외 이전 축의 변화가 없다 — 법적 선결 조건이 붙지 않는 이유다.\n\n[정정 2026-09-21] title 'CLOUD_HIGH 모델 핀 재평가 — Fable 5.1 교체 시 단가표·일일 한도표·프로바이더 인자 표면 동시 갱신' → 'CLOUD_HIGH 모델 핀 재평가 — 목표 Opus 5(2026-09-21 Kiki 지시·Fable 5.1 대체) + 구독 자격증명 사슬 복원': 2026-09-21 Kiki 지시로 CLOUD_HIGH 목표 핀이 Fable 5.1에서 Opus 5로 바뀜 + 구독 과금 경로가 현행 api_key 명시 주입에 막혀 자격증명 사슬 축이 추가됨. 설계 근거 = docs/architecture/03c_cloud_tier_transition_v2.md 4절. 신규 태스크 대신 amend를 쓴 이유 = 이 태스크가 이미 CLOUD_HIGH 핀을 소유하고 있어 신규 등재는 EOS-120형 중복 소유가 된다\n\n[정정 2026-09-21] 개발 단계 구독 경유 승인의 만료·재확인 지점 부착 (CLAUDE.md 만료 없는 유예 금지) — 설계 4.4절"
updated: 2026-09-21
