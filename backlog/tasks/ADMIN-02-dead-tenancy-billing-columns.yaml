# 빌드 하네스 태스크 — 상태 변경은 scripts/harness/backlog.py CLI 사용 권장
id: ADMIN-02-dead-tenancy-billing-columns
title: "dead 스키마 정직화 — school_id·subscription_* 4컬럼 소비처 0건 정리"
track: infra-debt
stage: S4
subject: cross
layer: backend
status: todo
priority: 3
eos_priority: P2
owner: claude
depends_on:
  - ADMIN-09-profile-collection-inventory-contract
requires_gates:
  - G-prod-dead-column-check
acceptance:
  - "① 현행 실측 고정 — school_id·subscription_tier·subscription_started_at·subscription_renewed_at 4컬럼 소비처 0건 재현 + prod DB 비영행 존재 여부 확인(비영이면 드롭 대신 미사용 자인 주석+동결 테스트로 범위 재조정 — Kiki 확인 필요)"
  - "② 드롭 마이그레이션 작성 + test_no_lockout_columns.py 컬럼 스냅샷 목록 동기화"
  - "③ 유지 대상(school_type·school_region·grade — 실 소비처 있는 학생 프로필 속성)이 함께 지워지지 않음을 동결"
  - "④ 전체 스위트 green(부분 스위트 통과를 전체 통과의 근거로 보고하지 않는다)"
  - "⑤ 범위 밖 — organization/school/license 테이블 신설은 하지 않는다(D4·§5-③)"
  - "③ [정정 2026-08-31·HARN-24] ②의 '드롭 마이그레이션 작성'은 **범위 무제한으로 읽으면 안 된다** — operations_platform_gap_review.md §독립검증 ②(main c3376c42)가 이미 범위를 좁혔는데 그 정정이 이 acceptance에 도달하지 못해, 정정을 조상으로 가진 세션이 ②를 그대로 집행해 subscription_* 3컬럼까지 드롭했다(b3a58b02). 드롭 대상은 ①의 prod 실측(G-prod-dead-column-check)에서 **비영 행이 0으로 확인된 컬럼만**이며, NOT NULL 행이 하나라도 있는 컬럼은 드롭이 아니라 '미사용 자인 주석 + 동결 테스트'로 처리한다."
artifacts: []
paths:
  - src/backend/whymath_backend/db/models/user.py
  - src/backend/whymath_backend/schema/user.py
  - "src/backend/alembic/versions/*"
  - tests/backend/api/test_no_lockout_columns.py
session: null
notes: "docs/architecture/operations_platform_gap_review.md §3 D2. user.py:93 school_id는 FK 없는 고아 컬럼, user.py:142-146 subscription_tier 등 3컬럼은 결제 시스템 부재로 읽는 코드 0건. account_security_gap_review.md:421 §4-⑧이 school_id 부재를 이미 자인했으나 정리는 안 됨. SEC-07이 좌석 없는 역할 3종은 안 만들었는데 같은 기준을 이미 있던 컬럼엔 적용 안 한 비대칭 해소. 반복 실수 8회차(§6) — 만들고 스키마를 읽지 않음.\n\n[차단 2026-08-11] 고립 브랜치 claude/admin-02-dead-tenancy-billing-columns(head d42fbd40)의 실측이 이 태스크의 전제를 반증한다 — 4컬럼 일괄 drop은 부정확하다. (1) prod DB(whymath-pg:5433) 비영행 확인이 샌드박스에서 불가(접근 경로 없음). (2) subscription_tier/started_at/renewed_at은 dead가 아니라 결제 도입 예약 좌석 — escalation_defaults.py가 '실 subscription_tier DB 읽기·결제 연동은 이 함수 내부만 바꾸면 됨'으로 명시한다. 재조정안: school_id만 drop 후보(forward 참조 0), subscription_* 3컬럼은 '예약·미배선' 자인 주석+동결로 유지. drop/annotate 확정과 prod DB 비영 여부는 Kiki 확인 필요(사람 게이트). 근거 브랜치는 이 사유 이관 후 삭제 대상 — docs/reviews/unmerged_branch_verdict_2026-08-11.md\n\n[차단 2026-08-11] 스코프 3분할 정정(operations_platform_gap_review_r2.md §4 D6) + Kiki 게이트 G-prod-dead-column-check 선결. (a) school_id = 드롭 후보 유지(FK 없는 고아 컬럼·D4가 organization으로 school을 흡수하므로 테넌시 착지 시에도 재사용 안 함). (b) subscription_tier/_started_at/_renewed_at = 드롭 금지 — dead가 아니라 결제 예약 좌석. l3/escalation_defaults.py:12,40이 미래 소비처를 자기선언했고 OPS-18(done) acceptance④가 '결제 도입 결정 이후'로 유예를 이미 판정했다. 처분은 예약 좌석 자인 주석 + 런타임 reader 0 동결 테스트. (c) 신규 발견 school_region(user.py:92)·gender(user.py:87)도 런타임 소비 0이자 API 수집 경로 0 — 단 dead code 축이 아니라 데이터 최소화 축(우선순위 2)이라 처분 근거는 신규 수집 항목 대장(D7)이 선행돼야 한다. ⚠ 현 acceptance ②('드롭 마이그레이션 작성')는 정정 전 4컬럼 일괄 스코프이며 acceptance 본문 정정은 amend CLI 부재로 불가(D8) — 착수 세션은 이 notes를 acceptance ②에 우선한다. ⚠ origin/claude/whymath-mvp-plan-architecture-trjg5x의 b3a58b02(4컬럼 전량 드롭, 2026-08-08)는 Kiki 판정으로 폐기 — 병합 금지(subscription_* 3컬럼까지 드롭해 OPS-18 유예 판정·escalation_defaults 자기서술과 충돌하고 마이그레이션 왕복 2회를 확정시킨다).\n\n[정정 2026-08-31] HARN-24 amend verb 신설에 따른 실피해 상환 — stale acceptance ② 정정 + 선결 게이트 부착(add 시점 부재로 못 붙였던 건)\n\n[정정 2026-09-01] eos_priority null → P2: 판정표 N/이월 — 2027 이월(폐기 아님·선언 §6-5). EOS-80 일괄 백필\n\n[정정 2026-09-06] HARN-53 분류: ADMIN-09 notes가 'ADMIN-02 (c)의 선결'이라 선언한 진짜 방향을 부착한다(처분 근거는 수집 항목 대장이 있어야 성립). 스캐너는 ADMIN-09 쪽에서 역방향으로 잡았으므로 그 쌍은 SOFT_DECLARED[REVERSED]로 분류"
updated: 2026-09-06
