# P-15 페르소나 3인 완주 판정 — 2026-09-19

> **판정 기준: main `433ec9ea`** (실행 시점 · 브랜치 `claude/modest-albattani-cp97rf`)
> **성격**: 계획서 300 Phase 2 §11 집행 지시문 `P-15`. 새 엔진을 추가하지 않고 대표 사용자
> 3명으로 전체 흐름을 반복 실행해 **안정화**하는 회차다. 소유 태스크 = `PED-36` acceptance ⑪.
> **실행 환경**: 실 PostgreSQL 16 + pgvector(`alembic upgrade head`) · mock LLM(코치는 LLM
> 호출 0) · shadow·judge·의미 매처 전부 기본 off.

---

## 1. 한 줄 판정

| 페르소나 | 완주 | 판정 근거 |
|---|---|---|
| **A 정상 학습자** | **부분 통과** | 문제·mastery 상승·다음 concept 전진은 관통. **"진단 → 학습" 화살표가 끊겨 있다** — 진단 확정이 쓰이지 않고(§4-D1) 학습 진입이 409로 막힌다(§3-A 6마디·실측) |
| **B 선수학습 결손** | **통과** | 반복 오답 → 선수 지목 → 하강 → 보정 → **원래 concept 복귀**까지 끝까지 감 |
| **C 특정 오개념** | **통과** | 오답 패턴 → 탐지 → 교정 개입 → targeted 문항 → **confidence 0.98 → 0.74(24.5% 감소)** |

원 문서 §11의 완료 판정은 *"3경로가 끝까지 돌고, Persona C의 confidence 감소가 수치로
보인다"*다. **C의 감소는 수치로 보인다**(§3-C). **A는 끝까지 돌지 않는다** — 그 끊긴 지점이
이 회차의 최대 산출물이며 `EOS-123`(P0)으로 등재했다.

---

## 2. 무엇을 만들었나

| 산출물 | 무엇 |
|---|---|
| `tests/backend/api/test_persona_three_path_completion.py` | 3경로 관통 하네스(실 PG · HTTP만) — 판정은 pytest exit code |
| `tests/infra/test_p15_persona_nightly_wiring.py` | 그 하네스가 **실제로 CI에서 도는지**의 배선 동결 |
| `.github/workflows/ci.yml` | `e2e-nightly` 잡에 P-15 스텝 1건 추가(실 PG·`WHYMATH_RUN_INTEGRATION=1`) |
| 이 문서 | 3경로 실행 기록 + LearnerState 변화표 + 발견 결함 목록 |

**Week 1~3 게이트와의 차이 — 학습자가 한 명이 아니다.** 기존 게이트 셋은 전부 고정 데모 계정
1인의 1회 관통이다. 그래서 "경로가 돈다"는 증명은 되지만 "*다른 상태의 학습자에게 서버가
다르게 반응한다*"는 증명은 되지 않는다(분기 자체가 관측되지 않는다). 이 하네스는 provider를
셋 등록해 **서로 다른 세 계정**을 만들고 같은 저작 콘텐츠 위에서 세 여정이 서로 다른 산출물을
받는지를 본다.

**학습자 상태는 한 줄도 직접 쓰지 않는다** — 전부 HTTP 부수효과다. ORM으로 심는 것은 저작
콘텐츠(concept·problem·problem_concept·skill_node·concept_edge·atom_node)뿐이고, DB를
*읽는* 것은 관측이지 개입이 아니다(오개념 confidence는 어떤 HTTP 표면도 숫자로 돌려주지
않는다 — §3-C 각주).

---

## 3. 3경로 실행 기록 + LearnerState 변화표

아래 표는 하네스가 표준출력에 남기는 `P15_LEARNER_STATE=` 줄을 그대로 옮긴 것이다(사람이
재구성하지 않는다 — 재구성하면 표와 실행이 어긋난다). `theta`가 전 구간 `None`인 것은 결함이며
`EOS-125`로 등재했다(§4-D4).

### A 정상 학습자 — `test_persona_a_normal_learner_completes_path`

| # | 마디 | 한 일 | mastery | 오개념 | recent_struggles | 스킬 |
|---|---|---|---|---|---|---|
| 0 | 계정 | 로그인(신규 upsert) | — | — | — | `learner_state` **0건** |
| 1 | 진단 | `DIAGNOSING` 전이 + CAT 출제(`action=diagnose`) | `{}` | `[]` | `[]` | 0건 |
| 2 | 문제 | main 개념 4문항(3정답 1오답) — 1문항은 **미응답으로 남김** | 0.920 | — | — | — |
| 3 | mastery 상승 | 부재 → 0.920 | `{m: 0.92}` | `[]` | `[]` | 1건 |
| 4 | 다음 concept | next 개념 1오답 후 약개념 가중 추천 | `{m: 0.92, n: 0.15}` | `[]` | `[n]` | 2건 |
| 5 | 진단 확정 | `POST /assessments/capture` → **written=False** | 동일 | `[]` | `[n]` | 2건 |
| 6 | 학습 진입 | `POST /objectives/{id}/study` → **409** | 동일 | `[]` | `[n]` | 2건 |

- 4)의 추천은 `weight_axes_applied=['weak_concept']`로 **next 개념 문항**을 골랐다. main 개념에
  **미응답 문항이 남아 있는데도** 다른 개념으로 갔으므로 "소진되어 그것밖에 없었다"와 구별된다
  (하네스가 그 미응답을 DB로 확인하고 단언한다 — 뮤테이션 M8이 이 절의 변별력을 봉인).
- 5)·6)이 이 회차의 핵심 발견이다 → §4-D1. **6)은 초판에서 "미판정"으로 남겼던 마디다** —
  초판은 "D1 때문에 도달할 수 없다"고 적었는데, 그것은 측정이 아니라 추론이었고 게다가 틀렸다:
  `post_study_unit`은 목표 로드(없으면 404)를 `require_learner_state`(없으면 409)보다 **먼저**
  하므로, 목표 행을 심지 않은 초판 픽스처에서 실제로 막혔을 지점은 D1이 아니라 404였다. 목표
  행을 심어 그 404를 배제한 뒤 재측정하니 **409**가 나왔다 — D1의 인과 사슬이 이제 추론이
  아니라 실측이다(뮤테이션 M12가 이 구별을 봉인한다: 목표 시딩을 빼면 404가 나서 RED).

### B 선수학습 결손 — `test_persona_b_prerequisite_gap_returns_to_original_concept`

| # | 마디 | 한 일 | mastery(pre / top) | recent_struggles | 판정 신호 |
|---|---|---|---|---|---|
| 1 | 반복 오답 | 후행 개념 3연속 오답 | — / 0.12 | `[top]` | 약점 확정 |
| 2 | prerequisite gap | 선수 1오답 후 `GET /weak-concepts/{top}/prerequisites` | 0.15 / 0.12 | `[top, pre]` | 선수 지목 `depth=1` · `weakness=0.0180` |
| 3 | 하위 concept | `GET /weak-concepts/{top}/coaching` | 0.15 / 0.12 | `[top, pre]` | **`focus=prerequisite_review`** |
| 4 | 보정 | 선수 문항 12회 정답 | **1.000** / 0.12 | `[top]` | 임계(0.7) 통과 |
| 5 | 원래 concept 복귀 | 선수 재조회 + 코칭 + 추천 | 1.000 / 0.12 | `[top]` | 선수 gap **0건** · `focus=foundation` · 추천=**top 개념 문항** |

- 요청 본문에는 선수 개념 id도 코칭 종류도 싣지 않는다. 서버가 후행 개념 id 하나만 받아
  `concept_edge` 역방향 순회 + BKT/IRT 약점 판정으로 **스스로** 선수를 골랐다.
- 5)의 추천이 "돌아갈 문항이 없어서"가 아님을 보장하려고 후행 문항을 4개 심고 3개만 썼다
  (처음 픽스처는 3개를 다 써서 `candidate_zero_reason=no_candidate_pool`로 끝났다 — 그 상태에서는
  "복귀하지 않았다"와 "돌아갈 곳이 없다"가 구별되지 않는다).
- **실측 함정**: `atom_node` 행을 안 심으면 선수 추천이 원자 축 울타리에서 조용히 제외되어
  **빈 배열**을 돌려준다. "선수가 안 막혔다"와 "축 밖이라 빠졌다"가 같은 응답이 된다(뮤테이션
  M1이 이 절을 봉인).

### C 특정 오개념 — `test_persona_c_misconception_confidence_decreases`

| # | 마디 | 한 일 | confidence | mastery | 오개념 |
|---|---|---|---|---|---|
| 1 | 오답 패턴 | `(x+2)^2 = x^2+4`를 **2회** 제출 | — | 0.12 | `[distribution-over-power]` |
| 2 | detection | 가설 실재 + 학습자 상태 노출 확인 | **0.9800** (증거 2건) | 0.12 | 동일 |
| 3 | corrective explanation | `POST /v1/coach/sessions` (중립 발화만) | **0.8500** | 0.12 | 동일 |
| 4 | targeted problem | 미응답 문항 추천 → **정답** 제출 | **0.8500(불변)** | 0.44 | 동일 |
| 5 | confidence 감소 | 증거 없는 코치 턴 1회 | **0.7400** | 0.44 | 동일 |

- **감소폭 0.2400 · 상대 24.5%.** 원 문서 §11-②의 판정 포인트("올라가기만 하면 보정이 작동
  안 한 것")를 충족한다.
- 요청 본문에 오개념 id도 힌트 단계도 싣지 않았고, 3)의 교정 개입은
  `misconception_id=distribution-over-power`를 **서버가 지목**해 돌려줬다(`pattern=counterexample`).
- 4)의 **불변**이 결함이다 → §4-D2. 정답 시도는 감쇠를 트리거하지 않는다.
- 각주: 개입의 `escalation_rung`은 `exclude=True`라 HTTP에 **일부러** 실리지 않는다(학생 대면
  발화를 누적 횟수의 함수로 만들지 않기 위한 설계). 그래서 하네스는 노출되는 축
  (`misconception_id`·`pattern`·`prompt`)으로 단언한다. `confidence` 역시 어떤 HTTP 표면도
  숫자로 주지 않으므로 그 축만 DB 관측을 쓴다.

---

## 4. 발견한 결함 — 고친 것 / 등재한 것

P-15 ④ 범위 규율("발견한 결함은 **고치되 범위를 넓히지 마라**. 새 기능이 필요하다고 판단되면
태스크로 등재만 하고 보고하라")을 그대로 적용했다. **프로덕션 코드는 한 줄도 바꾸지 않았다** —
네 건 전부 처분 판단이 필요한 설계 축이라 등재만 했다.

| # | 결함 | 처분 | 태스크 |
|---|---|---|---|
| **D1** | **CAT 중단 규칙 미도달** — `TARGET_SE=0.3`인데 45문항을 풀어도 SE가 0.391에서 멈춘다(14문항 0.685·후반 문항당 0.0005 수렴). 그래서 `assessments/capture`가 영구히 `insufficient_measurement`이고, 그 분기에서만 도는 `provision_learner_state`가 실행되지 않아 **`learner_state` 행이 생기지 않는다**. 결과: `POST /v1/me/objectives/{id}/study`가 영구히 409("학습을 시작하려면 먼저 진단을 완료해야 합니다") | **등재** (P0) | `EOS-123` |
| **D2** | **정답 시도가 오개념 감쇠를 트리거하지 않는다** — 오개념 훑기가 정답이면 `NOT_RUN`이라 `apply_candidates` 호출 조건에서 빠진다. 감쇠를 실제로 일으키는 것은 *또 다른 오답*과 *코치 턴*뿐. 교정 설명을 듣고 targeted 문항을 맞힌 사건만 신뢰에 아무 영향도 주지 않는다 | **등재** (P1) | `MISC-34` |
| **D3** | **상태 머신 R4(선수결손)가 서빙에서 미발화** — `build_attempt_evidence`가 `prerequisite_gap_concept_ids`를 인자로 열어 뒀으나 유일한 호출부가 넘기지 않는다. `EOS-105`가 한계로 자인했으나 **승계 태스크가 없었다**. 선수 하강은 조회형 3종으로만 살아 있다(그래서 B 경로는 통과한다) | **등재** (P1) | `EOS-124` |
| **D4** | **루프 학습자의 `general_ability`가 항상 null** — 생산자가 `ability_snapshot` 최신 행을 읽는데 그 행을 만드는 유일한 경로가 수동 캡처 엔드포인트다. 같은 순간 `/next-problem`은 θ를 계산해 응답에 싣는다 — 두 표면이 같은 학습자에 대해 다른 말을 한다 | **등재** (P1) | `EOS-125` |

**고친 것**: 없다(프로덕션). 하네스 자신의 결함 2건은 발견 즉시 고쳤다 — ⓐ 5단계 감소 단언이
마지막 코치 턴에 의존하지 않아 그 턴을 생략해도 통과했다(뮤테이션 M5로 발각 → *이 턴의 기여*를
따로 단언) ⓑ A-4 마디가 main 문항 소진으로 자동 통과했다(→ 1문항 미응답 보존 + DB 확인).

---

## 5. 보호 장치 검증 — 뮤테이션 13종 전건 RED

정상 입력에서 초록인 것은 증거가 아니므로, 막으려는 상태를 실제로 주입해 RED를 확인했다.
하네스는 순수 Python(셸 배제 — 백틱·이스케이프 사고 회피)이며 **주입이 실제로 적용됐는지
(`mutated != original` + 치환 대상 1건 + 길이 산술)와 원복이 바이트 동일한지(sha256)를 각각
단언**한다.

| # | 주입 | 대상 | 결과 |
|---|---|---|---|
| M1 | `atom_node` 미시딩 → 선수가 원자 축 밖으로 제외 | B | RED |
| M2 | 선수 엣지 방향 반전 | B | RED |
| M3 | 선수 보정 생략 | B | RED |
| M4 | 오답 1회만(사다리가 교정 등급 미달) | C | RED |
| M5 | 마지막 코치 턴 생략 | C | RED |
| M6 | 사정거리 밖 오답(가설 자체가 안 섬) | C | RED |
| M7 | 전건 오답(mastery 상승 없음) | A | RED |
| M8 | main 문항 소진(전진/소진 구별 불가 상태) | A | RED |
| M9 | 선수 미측정(결손과 모름의 구별) | B | RED |
| M10 | **서버**: `decay` 무력화 | C | RED |
| M11 | **서버**: 선수 traversal 방향 반전 | B | RED |
| M12 | 학습목표 미시딩 → 409가 아니라 404(차단 사유 혼동) | A | RED |
| MW1 | **배선**: `ci.yml` 스텝이 다른 파일을 돌게 교체 | 배선 가드 | RED |

**생존한 주입 하나를 제거로 처리했다.** §A-6에 처음에는 `status_code != 404`를 별도 절로 뒀는데,
그 절을 지우는 주입(M13)이 **초록**이었다 — 당연하다. 뒤따르는 `== 409`가 404를 이미 배제하므로
그 절은 어떤 입력에서도 판정을 바꾸지 못하는 **검출력 0의 절**이었다. 픽스처가 그 절에 닿지
못한 것이 아니라 절 자체가 비어 있었으므로, 픽스처를 고치는 대신 절을 없애고 진단 가치는
실패 메시지로 옮겼다(보호처럼 보이지만 보호가 아닌 절은 남기지 않는다).

**전건 RED를 커버리지 달성으로 읽지 않는다**(CLAUDE.md v0.2.19 확장). 이 숫자가 말하는 것은
"내가 주입 목록에 올린 13가지 실패를 검출한다"이지 "검출하지 못하는 실패가 없다"가 아니다.
실제로 M5는 **처음 설계에 없었다면 통과했을** 결함을 잡아냈다 — 목록에 올리지 못한 실패 모드는
여전히 남아 있다고 보아야 한다.

---

## 6. 정직한 공백

1. **학습 단위 *공급*은 판정하지 않았다** — 진단 게이트(409)까지는 §A-6이 측정하지만 그 뒤의
   교수법 선택·DSL 렌더는 지나가지 않는다. 현행 main에서 게이트가 열리지 않기 때문이고, 열리더라도
   DSL 적재라는 별개 전제가 붙는다. **후보 풀이 이 회차의 콘텐츠뿐이라는 가정도 함께 적어 둔다** —
   §A-4·§B-5·§C-4가 추천된 *문항 id*를 단언하므로 같은 DB에 다른 회차의 잔여 콘텐츠가 있으면
   거짓 실패가 난다(로컬 실측: 중단된 탐침이 남긴 문항 6건이 §C-4를 거짓 실패시켰다). CI는
   잡마다 새 postgres 서비스를 띄우고 이 하네스는 성패와 무관하게 자기 콘텐츠를 지운다.
2. **연속 3루프(계획서 §18 최중요 조건)는 이 회차의 범위가 아니다** — `PED-36` ⑫가 소유한다.
   이 하네스는 각 페르소나를 1회 관통한다.
3. **`SCENARIO-001~010` 정형화(계획서 §16)도 범위 밖** — `PED-36` ⑧·⑩ 축이며, 2026-09-19
   시점 그 두 축은 `EOS-119`(타 세션 원격 claim)와 **중복 소유** 상태다(`EOS-120`이 그 판정과
   해소 경로 3택을 소유). 이 PR은 ⑪(페르소나 3인)만 다루고 ⑧·⑩을 건드리지 않는다.
4. **교수학적 품질은 보지 않았다** — 코칭 문구·교정 발화의 적절성은 판정 대상이 아니다
   (연결성과 수치 변화만). 그 축은 judge 게이트가 소유한다.
5. **CI에서 아직 한 번도 돌지 않았다** — 배선은 했고 그 배선의 실재성은 기계로 동결했지만,
   `e2e-nightly`는 schedule 전용이라 이 PR의 CI 실행에서는 스킵된다. 첫 실제 실행은 다음
   야간이다. 로컬 실 PG 실행은 위 표가 그 기록이다.

---

## 7. 다음 항목(`P-16` Gate 2 판정)에 넘기는 선행 조건

- **`EOS-123`(D1)이 P-16의 조건 3·10에 직접 걸린다.** §18 조건 3은 "LearnerState 자동 생성",
  조건 10은 "전체 과정 반복 가능"인데 둘 다 진단 확정을 전제한다. P-16 판정 전에 D1의 처분이
  나와 있어야 하며, 나오지 않으면 그 두 조건은 **미충족**으로 판정해야 한다(조건부 PASS 없음).
- **연속 3루프 실증(`PED-36` ⑫)이 아직 없다.** §18이 "가장 중요"라고 지목한 조건이므로 P-16
  이전에 별도 회차가 필요하다.
- 이 하네스의 3경로는 P-16의 조건 1·2·4·5·6·7·8·9에 대한 실행 증거로 인용할 수 있다(단
  조건 2 "진단 완료"는 D1 때문에 인용 불가).

---

**작성**: 2026-09-19 · 소유 태스크 `PED-36` acceptance ⑪ · 집행 지시문 `P-15`
