# P3-00a trunk 검증 — 3회차 착지 확인 + Kiki 결정 2건 집행 + `EOS-128` 종결

**판정 기준: main `1671b9d4d88434a3d93bfcaa93d52c34738bd55e`** (이 세션 브랜치의 분기 기준 · 2026-09-23)
검증 시점 `origin/main`은 `ade56c4845c912aa9892c4e7343121d7becc99d4`까지 전진해 있다(타 세션 push). 산출물 실재 검사는 전진분을 포함한 `origin/main` 기준으로 했고, **분기 기준과 검증 기준을 열을 나눠** 아래에 적는다.
세션 `claude/docs-p3-00a-verification-17ea81` · 태스크 `EOS-128-phase3-plan-backlog-conversion` 4회차

---

## §0. 이 세션이 이 태스크를 다시 실행하지 않은 이유

지시문 `01_P3-00a`는 P3-00a를 미착수로 전제한다. 그러나 trunk 실측 결과 세 차례에 걸쳐 이미 착지했다.

| 회차 | 증적 | 내용 |
|---|---|---|
| 1회차 | PR #1233 | Not Now 12종 보류 등재(신규 11 + `E1-01` amend) + 게이트 `G-not-now-v1-release-recheck` |
| 2회차 | PR #1276 | 대조표 `docs/strategy/phase3_math_eos_completion_backlog_crosswalk.md` §10~§15 + 원 문서 전사 보존 |
| 3회차 | PR #1276 (`main 5afc1c34`) | 20 회차 수령 → 신규 14 + amend 1(`EOS-50`) + 게이트 5. 대조표 §21에 acceptance ①~⑤ 충족 |

**재실행은 지시문 자신의 acceptance ②(중복 실측)를 위반한다.** 그래서 이 세션의 일은 셋이다 — (a) trunk 기준 **검증** (b) **Kiki 결정 2건 집행** (c) **`EOS-128` 종결**.

---

## §1. acceptance ①~⑤ 항목별 재대조

대조표 §21의 자기 판정을 신뢰하지 않고, `EOS-128` acceptance **원 문면**과 1:1로 대조했다.

| 축 | 원 acceptance 요구 | 판정 | 실측 근거 |
|---|---|---|---|
| ① | 원 문서 대조 — Week 1~4 작업·지표 7종·Release Gate A~E를 착수 단위로 분해, 원 문서 각 항목 ↔ 태스크 ID 대조표를 `docs/strategy/` 아래에 | **충족** | 대조표 §18-1이 20회차 ↔ 태스크 ID를 1:1로 잇는다(착수 15 + 판정 5). §11~§13이 주 39건 + 세부 36행. **1회차 미이행분**(지시문 1건만 첨부)은 §0-1에 "등재할 수 없었던 항목"으로 기록돼 있고, 3회차에 입력이 도착해 해소됐다 |
| ② | 중복 실측 — 기존 태스크 전수 조회 + `git log --all --grep`(unshallow 후)로 미머지 구현 확인, 겹치면 `amend` | **충족** | 회차마다 역할 정규식 + 코드 실측. 겹친 3건(`P3-11`→`EOS-50` amend · `P3-12`→`ADMIN-03` 통합 축 + 선행 · `P3-13`→`EOS-117` 선행)은 신규 등재하지 않았다(§18-2) |
| ③ | 신규 등재는 `backlog.py add`로만 · 선행은 notes 산문이 아니라 `--depends` full-id | **충족** | 번호 추론 0건(`HARN-135` 충돌 시 CLI 제안 번호 사용 이력) · 선행 전건 `--depends` · `audit-deps` green(위반 0건·이 세션 재실행 확인) |
| ④ | Not Now 12종 등재(notes "v1.0 이후, 11월 착수 금지"·priority 5·`eos_priority` P3) + 재확인 지점 게이트 부착 + Not Now ∩ 진행중 보고 | **충족(1회차)** | §4 참조 — 11 태스크 + `E1-01` amend + 게이트 1. ∩ 진행중 **0건** |
| ⑤ | 산출 4종(등재 목록·대조표·Not Now∩진행중·미등재 항목) · 대조표 상단에 판정 기준 해시 · PR | **충족** | §2·§4·§7 및 대조표 §19·§18-1·§3·§21-1 |

**①의 판정 기준 해시에 관한 실측 정정 1건**: 3회차가 대조표 상단에 박은 `판정 기준: main 5afc1c34`는 **머지 전 해시**다. 측정 결과 `5afc1c34` 시점의 대조표는 **2회차 판본**이고(239행 · 판정 기준 `e91a75d3` · 2026-09-20), `§18`~`§21` 섹션이 **하나도 없다** — 즉 3회차의 자기 판정(§21)은 그 해시에 존재하지 않는다. 그 판정이 서술한 산출물은 커밋 `fb020c1c`("EOS-128 — … 전 20회차 백로그 변환 (신규 15 · amend 2 · 게이트 5) (#1276)")로 착지했고, 대조표 blob은 `1974a2b6` → `d048fe6f`로 바뀌었다.

이 사실이 중요한 이유는 **§21은 독립적으로 재현할 수 있는 판정이 아니라 작업과 함께 쓰인 자기 판정**이라는 점이다. 그래서 이 보고서는 §21을 인용하지 않고 `EOS-128` acceptance **원 문면**과 1:1로 대조했고(위 표), 그 결과가 §21과 일치함을 확인했다. "미머지 존재를 충족으로 단정 금지" 규칙이 경계하는 형태이지만 여기서는 무해하다 — 기준 해시가 머지 전인 이유는 그 커밋이 **산출물 그 자체**였기 때문이고, 착지 후 실재는 §2가 전수로 확인했다.

---

## §2. 산출물 실재 검증 (30건 전수 · 누락 0건)

`git cat-file -e origin/main:<path>`로 검사했다. **검사 대상 30건 · 누락 0건**(exit code 판정).

| 묶음 | 건수 | 비고 |
|---|---|---|
| 신규 P3 태스크 | 14 | `P3-00`·`P3-01`~`P3-10`·`P3-12`·`P3-13`·`P3-14` — **전건 `status: todo`**(착수 0건) |
| 사고 대책 태스크 | 1 | `HARN-136`(git 원복 차단 훅) |
| Not Now 태스크 | 11 | §4 |
| 문서 | 4 | 대조표 · 지시문 보존본 · 원 문서 보존본 · `EOS-22` 판정문 |

**`P3-11`에는 `P3-` 태스크가 없다** — `EOS-50-publish-gate-pipeline`이 그 회차 소유자다(3회차 amend). 이는 §18-1이 "ID 표기 주의 ⓑ"로 이미 기록한 사항이다.

전건 `todo`라는 사실이 중요하다 — **Phase 3 작업은 한 건도 시작되지 않았다.** 따라서 이 태스크의 "재실행 금지" 판단은 미착수 상태의 착지물에 대한 것이고, 병렬 세션이 같은 회차를 구현 중이라는 가능성은 배제된다(원격 claim 10건 중 P3·NOTNOW 계열 0건).

---

## §3. 2평면 지표 분리 — 저장소 KPI 12종 ↔ 원 문서 7종

원 문서의 지표 7종(Curriculum Coverage · Concept Completeness · Problem Coverage · Solution QA Pass · Learning Loop Success · Critical Defect · Graph Connectivity Coverage)은 저장소의 KPI 12종과 **다른 평면**이다. 저장소 12종은 생산 공정(CU당 시간·비용·오류율)이고, 원 문서 7종은 제품 완성도다.

이 분리는 `P3-14-phase3-metrics-cli` acceptance **③④⑤**에 실재한다 — ③이 "기존 KPI 정본 12종과 2평면으로 분리, 정본이 셋이 되지 않게 이름·문서에 평면을 명시(Kiki 수용 2026-09-22)", ④가 중복 2종(Solution QA Pass·Critical Defect)의 흡수, ⑤가 Concept Completeness의 재설계 근거다.

즉 **평면 구분은 등재 시점에 이미 집행돼 있다.** 다만 ③의 "정본이 셋이 되지 않게"는 `P3-14` 착수 시점에 판정될 사항이며, 이 보고서는 그 판정을 대신하지 않는다.

---

## §4. Not Now 12종 재확인 — 11 태스크 + `NOTNOW-06` 의도적 공백

| 축 | 실측 |
|---|---|
| 태스크 | `NOTNOW-01`~`05`·`07`~`12` = **11건**, 전건 `todo` · `priority: 5` · `eos_priority: P3` |
| `NOTNOW-06` | **파일 부재** — 의도적 공백(12종 목록의 6번이 태스크화되지 않음) |
| 12번째 | `E1-01-dimensional-consistency` amend(`todo` · `priority: 1` · `P3`) |
| 보류 게이트 | `G-not-now-v1-release-recheck` `pending` · `remind_after_days: 100` — "v1.0(12월 출시) 이후 첫 계획 세션에서 재개·폐기·유지 3택 판정" |
| **Not Now ∩ 진행중** | **0건** — 11건 전부 `todo`, 원격 claim 10건 중 NOTNOW 계열 0건, 열린 PR 11건 중 NOTNOW·`E1-01` 계열 0건(실측) |

`E1-01`의 `priority: 1`은 요구("priority 5")와 다르다. 이는 **의도된 예외**다 — `E1-01`은 원래부터 있던 실제 태스크(차원 일관성)이고 Not Now 항목을 *흡수*한 것이지 Not Now로 *강등*된 것이 아니다. 태스크를 두 개로 쪼개지 않은 이유는 1회차 등재 시 기록돼 있다.

**게이트 문면의 stale 1건**: `G-not-now-v1-release-recheck`의 제목에 "Phase 3 기간 2026-10-26~11-22"가 남아 있는데, 이 세션의 결정 ⓐ로 Phase 3 실효 기간이 **10/26~11/15**로 바뀌었다. 보류 *의미*(11월 착수 금지)는 영향받지 않으므로 그대로 두고, 이 사실을 `HARN-124`(게이트 문면 정정 경로)의 실사례로 적는다 — 정정 CLI가 없어 여기서 손대지 않았다.

---

## §5. 이번 세션 집행 — Kiki 결정 2건

### 5-1. ⓐ 종료일 충돌 → 저장소 `G3` 우선 · P3-G4를 11/15로 옮겨 P3-G3와 통합

대조표 §14-5·§16-3 ③이 "일정 우선순위는 세션이 정할 사안이 아니다"로 Kiki 미결로 남긴 건이다. 결정을 집행한 결과:

| 게이트 | 처분 |
|---|---|
| `G-p3-w3-release-merged` | **신규**(11/15) — Week 3 판정 + Phase 3 Release Gate A~E 판정을 한 회차로 통합. `kind: decision` · `assignee: kiki` · `remind_after_days: 55` |
| `G-p3-g3-week3` | **waived** — 요건은 통합 게이트 (가)축으로 승계 |
| `G-p3-g4-release` | **waived** — 요건은 통합 게이트 (나)축으로 승계 |

`gates` 서브커맨드는 `list/add/clear/waive/show` 5종이고 **정정 CLI(`amend`)가 없다.** 게이트 날짜·문면은 제목 문자열에만 있어, 손편집 금지 규율 아래에서 `waive + add`가 유일한 경로였다. Waive 사유에 "요건은 폐기가 아니라 이동"을 적어 보존했다.

**집행 중 발견한 구조 결함 1건 — 병합이 순환을 만들었다.** 통합 게이트의 Release 축은 Week 4 작업 묶음(P3-11~P3-14)이 완료된 시점에 판정되는 것을 전제하는데, 그 묶음의 첫 작업(`EOS-50` = P3-11)이 통합 게이트를 `requires_gates`로 걸고 있었다. 즉 11/15에 Release 축을 판정하려면 P3-11이 완료돼 있어야 하는데 P3-11은 그 게이트가 통과해야 착수된다 — **자기 자신을 전제로 요구하는 순환**이라 통합 게이트를 정상 경로로 닫을 수 없다. 원 게이트 2건에는 이 순환이 없었다: Week 3 게이트는 Week 4의 *진입* 조건, Release 게이트는 Week 4 *이후* 판정이라 역할이 분리돼 있었다. 병합이 진입 조건과 완료 요건을 같은 게이트에 겹치게 했다.

`EOS-50`의 `requires_gates`에서 통합 게이트를 떼어 순환을 끊었다. Week 4 착수 경계는 `depends_on`이 이미 지킨다(`P3-10-ai-quality-eval-suite` = Week 3 작업 완료가 선행). **게이트가 하던 일을 의존 체인이 하므로 요건 손실은 없다.**

**부수 결과(정직 기록)**: Phase 3 실효 기간이 10/26~11/15로 줄어 Week 4 작업이 압축된다 — **구현은 11/14까지 끝나야 한다.** 이것이 수용 불가라면 Release 판정을 11/22로 되돌리는 것이 유일한 대안이며, 그것은 Kiki 결정을 뒤집는 것이므로 세션이 정하지 않았다.

### 5-2. ⓑ Phase 2 Gate 2 FAIL → 지금 즉시 재판정 착수

**재판정 태스크는 실재하지 않았다.** 다만 이름으로 찾으면 0건이고 기능으로 찾으면 두 태스크가 나오므로, 그 둘이 재판정이 *아님*을 확인하는 것이 실측의 본체였다:

- `EOS-22-gate2-final-judgment` — `done` · 최종 FAIL. **1회차 판정**이고 후속이 없다.
- `P3-00-phase2-acceptance-check` — 제목에 "Gate 2 10조건·KPI 5종 main 기준 재실측"이 있어 재판정으로 오독하기 쉬우나, 그 태스크 자신의 notes가 "**등재는 착수가 아니다 — 착수 전제는 Phase 2 Gate 2 PASS**(게이트 `G-p3-entry-gate2-pass`)"라고 적어 진입 **후** 인수 점검임을 밝히고 있다. 게다가 `requires_gates`로 그 진입 게이트를 걸고 있어 **자기가 증거를 대야 할 게이트에 막힌다** — 재판정 소유자가 될 수 없다.

그래서 신규 등재했다.

| 항목 | 값 |
|---|---|
| ID | `EOS-130-phase2-gate2-rejudgment` |
| 등급 | `eos_priority: P0` — 정의("없으면 12월 검증(G0~G5)이 성립하지 않는다")에 해당. Gate 2는 저장소 G2(기준선 확보)의 판정 대상이고 재판정 없이는 G2 verdict가 2026-09-19 FAIL로 고정된다. P0 예산 50건 대비 비종결 P0 5건이라 교환 불필요 |
| 선행 | `depends_on: EOS-21-week-gate-harnesses-never-run-in-ci` — `EOS-22`의 유일 미충족 조건이 "무개입 연속 3루프"이고 그 루프의 판정 하네스 중 3종이 어느 CI 잡에서도 실행되지 않는다(`EOS-21` acceptance 확장이 워크플로 참조 0건으로 실측) |
| 게이트 | **미부착** — 걸면 자기가 증거를 대야 할 진입 게이트에 막혀 `P3-00`과 같은 순환이 된다. 재판정은 진입 게이트 *앞*에서 도는 유일한 Phase 3 관련 작업이다 |

**등급 불일치를 은폐하지 않고 적는다**: 원 판정 `EOS-22`는 `P2`로 등급돼 있었다. 이 등급은 그와 다르다. 등급 불일치는 맞출 대상이 아니라 보고 대상이다.

**"즉시 착수"의 실현 형태**: `EOS-21`은 `depends_on`·`requires_gates`가 모두 비어 **지금 착수 가능**하다. 임계 경로는 `EOS-21` 착지 → 재판정이다. 재판정을 배선 상태를 모른 채 돌리면 같은 FAIL을 반복하므로 선행을 걸었고, 그 결과 "즉시"는 재판정 단독으로는 성립하지 않는다 — 이것이 결정의 실제 비용이다.

**게이트 clear는 Kiki 소유다.** `G-p3-entry-gate2-pass`는 `kind: decision` · `assignee: kiki`이고, 재판정 태스크는 그 게이트의 *증거를 만드는* 것이지 닫는 것이 아니다. 에이전트가 `--as kiki`로 대행하지 않았다(거부의 우회 금지).

---

## §6. 대장 변경 내역 (이 세션)

| 종류 | ID | 처분 |
|---|---|---|
| add | `EOS-130-phase2-gate2-rejudgment` | ⓑ 집행 |
| add | `G-p3-w3-release-merged` | ⓐ 집행 |
| waive | `G-p3-g3-week3` · `G-p3-g4-release` | ⓐ 집행(요건 승계) |
| amend | `EOS-50-publish-gate-pipeline` | 게이트 재지정 1회 + 순환 해소 1회 |
| amend | `HARN-124-gate-title-correction-path` | 실측 트리거 2건 편입(⑧⑨) |
| cancel | `HARN-167-gates-amend-missing` | **중복 등재 취소** |

### 6-1. 중복 등재 1건 — `HARN-167`(구 `HARN-162`) 취소

`gates`에 `amend`가 없음을 실측하고 "게이트 정정 경로가 없다"로 결론해 `HARN-167-gates-amend-missing`을 등재했는데, 등재 직후 harness의 의미 중복 고지가 `HARN-124-gate-title-correction-path`를 similarity 0.15로 지목했다. 확인 결과 **같은 기능을 이미 소유**하고 있었다(2026-09-21 등재 · acceptance ①이 `gates`에 정정 경로를 여는 것을 확정 · ⑤⑥⑦이 `remind_after_days`·독촉 기준일 축까지 편입).

원인은 **부재 판정의 검색 방법 오류**다 — CLI 표면(도구)만 확인하고 태스크 대장(소유자)을 확인하지 않았다. 계보는 3대째다: `HARN-133` → `HARN-135` → `HARN-160`이 등재됐다가 `HARN-124`에 편입·취소됐고, **그 태스크의 notes가 "두 태스크가 각각 경로를 열면 중복이 된다"고 적어 둔 바로 그 중복**을 재생산했다.

새 증거는 취소로 버리지 않고 `HARN-124` acceptance ⑧(2026-09-23 통합 집행이 같은 벽에 부딪힌 두 번째 트리거)과 ⑨(이번 중복 등재를 그 태스크의 자기 사례화)로 편입했다.

**재발방지대책 형태**: 새 산문 규칙을 만들지 않았다 — 2026-09-20 Kiki 지정 「새 산문 규칙 등재 동결」에 따라 대책은 **태스크**(`HARN-124` ⑨)와 **MEMORY 결정 로그**로만 등재한다.

---

## §7. 미등재 잔여 4건의 처분 결과

| # | 항목 | 이 세션의 처분 |
|---|---|---|
| ⓐ | 종료일 11/22 ↔ 저장소 `G3` 충돌 | **처분 완료** — §5-1. 게이트 `G-p3-w3-release-merged`(11/15) + waive 2건 |
| ⓑ | Phase 번호 체계 충돌 | **처분 완료** — §5-2. `EOS-130` 등재. 대조표 §21-1 ②가 "같은 것으로 읽힌다, 다만 읽어서 그렇게 보인다이지 실측은 아니다"로 남긴 부분은 **여전히 실측되지 않았다** — 이 보고서는 그 축을 닫지 않는다 |
| ⓒ | 금지5(대규모 UI 구조 변경)의 기계 집행 | **승계 유지** — `HARN-103`(todo·미착지)이 소유자. 신규 등재하면 두 번째 사본이 된다 |
| ⓓ | 상시 지시문 `S-01`~`S-03` | **승계 유지** — 세션 운영 규율이지 착수 단위가 아니다. 저장소 집행은 Not Now 12종·좌석 계약·스키마 프로브·`entry_gate`가 가진다(`HARN-103`의 금지5만 공백) |

**추가 발견(ⓑ의 부산물)**: `G-p3-entry-gate2-pass`가 막는 태스크 4건 중 `P3-00`은 "인수 점검"이라 진입 후 작업인데 그 게이트에 막혀 있다. 이는 순환이 아니라 **의도된 순서**다(진입 → 인수 점검). 재판정(`EOS-130`)이 진입 게이트 앞에 서고 `P3-00`이 뒤에 선다.

---

## §8. 한계 (명시)

1. **이 보고서는 검증이지 신규 등재가 아니다.** Phase 3 착수 단위의 등재는 3회차에 끝났고, 이 세션은 그 실재와 acceptance 충족을 재확인한 것 + Kiki 결정 2건을 집행한 것이다.
2. **판정에는 시점이 붙는다.** 기준은 `1671b9d4`이고 검증 시점 `origin/main`은 `ade56c48`이다. 두 해시가 다른 이유는 타 세션의 push이며, 3회차 산출물 30건은 두 기준 모두에서 실재한다.
3. **`ⓑ`의 "체계 vs 날짜" 축은 실측되지 않았다.** §7 ⓑ 참조.
4. **게이트 문면 정정은 이 세션에서 하지 않았다.** `G-not-now-v1-release-recheck`의 stale 기간 표기(§4)와 병합으로 늘어난 게이트 ID 3개(§5-1)는 `HARN-124`가 열 때 함께 정리한다.
5. **CI 검증은 별건이다.** 이 세션의 변경은 대장 YAML·문서라 pytest 대상이 아니지만, PR의 CI 잡 스텝 목록(`harness-integrity`)을 **열어 놓고** 로컬 검증을 그 목록에 맞췄다 — pytest만 돌리고 green이라 말하지 않는다. 돌린 것: `validate` · `audit-deps` · `rules lint` · `rules render --check` · `jit check` · `adr_number_check.py`(전 브랜치 fetch 후) · `claims reap`(dry-run) · `branches` · `ruff check scripts tests/harness` · `black --check --line-length 100 scripts tests/harness` · `pytest tests/harness` — 전건 exit 0.

6. **`pytest tests/harness`는 3건 red다 — 회귀가 아니라 선재 환경 결함이며, 실측으로 판정했다.** `3 failed / 970 passed / 1 skipped`. 실행을 백그라운드 래퍼로 감쌌기 때문에 **하네스 완료 알림의 exit code는 `echo`의 0이었고**, 판정은 로그의 `PYTEST_EXIT=1` 줄로 했다(「래퍼가 종료 코드를 가리는 축」). 원인은 `tests/harness/test_cli.py:28`의 테스트-로컬 `_run_git`이 `subprocess.run(..., text=True)`로 인코딩을 지정하지 않아, 로케일 cp949 환경에서 git이 내는 UTF-8 한글을 reader 스레드가 디코드하다 `UnicodeDecodeError`를 내는 것이다(실패가 단언이 아니라 스레드 예외라 그 테스트의 목적이 검증되지 않은 채 red로만 남는다).

   **판정 근거(실측)**: 분기 기준 `1671b9d4`에 프로브 워크트리를 만들어 같은 테스트를 돌린 결과 **동일하게 3 failed / 2 passed** — 이 세션의 대장 변경이 원인이 아니다(프로브는 정리했다). 기존 소유자를 이름이 아니라 **역할로** 검색한 결과 이 축은 비어 있었다: `HARN-19`(done)는 `scripts/harness/*`를 고치고 acceptance ⑥에서 "scripts/ 밖은 범위 밖"으로 명시 제외했고, `OPS-53`은 *출력* 축, `OPS-44`는 같은 현상을 다뤘으나 paths가 `tests/backend/**`였다. → **`HARN-166`으로 등재**(infra-debt·P3). 등재 시 CLI가 제안한 번호는 `HARN-163` 충돌로 **`HARN-166`**이었다(번호 추론 금지 규칙이 작동).

---

## §9. `EOS-128` 종결 판정

acceptance ①~⑤ 전건 충족(§1) · 산출물 실재(§2) · 미등재 잔여 4건 전부 처분 또는 승계 명시(§7).

**`EOS-128`은 `done` 처리 가능하다.** 3회차가 "①②가 Kiki 결정이므로 done 판정 자체도 Kiki 확인 후에 한다"로 남긴 조건은 2026-09-22 Kiki 결정 2건으로 해소됐고, 그 집행이 이 보고서다.

---

**작성**: 2026-09-23 · `EOS-128-phase3-plan-backlog-conversion` 4회차 · 세션 `claude/docs-p3-00a-verification-17ea81` · 판정 기준 main `1671b9d4`